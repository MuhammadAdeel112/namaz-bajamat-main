import 'package:flutter/material.dart';

class UpdateImamProfileScreen extends StatelessWidget {
  const UpdateImamProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Imam Update"));
  }
}
